name: foodre_delivery
description: Замовляй онлайн - забирай офлайн

publish_to: 'none'

version: 1.1.2+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.1

  firebase: ^8.0.0
  firebase_core: ^0.7.0
  firebase_auth: ^0.20.0+1
  cloud_firestore: ^0.16.0
  firebase_storage: ^7.0.0

  hive: ^1.4.4+1
  hive_flutter: ^0.3.1
  path_provider: ^1.6.27
  provider: ^4.3.3

  transparent_image: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  hive_generator: ^0.8.2

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/icons/